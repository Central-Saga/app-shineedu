# Screenshots - UI Refresh

- **login.png** – Halaman Login (bg-slate-50, Card rounded-2xl, logo/fallback)

Untuk **Users**, **Roles (Matrix)**, dan **Permissions**: jalankan app (`npm run dev`), login dengan akun yang punya akses, lalu buka `/users`, `/roles`, `/permissions` dan ambil screenshot.
